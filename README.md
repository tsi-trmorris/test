# newrepo

CLI project scaffolded with `bun init`. No features yet.

## Setup

```bash
bun install
```

## Run

```bash
bun run start
# or
bun run index.ts
```

This project was created using `bun init` in bun v1.4.2. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
